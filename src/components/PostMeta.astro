---
interface Props {
  date: Date;
  tags?: string[];
}

const { date, tags } = Astro.props;

const formatted = new Intl.DateTimeFormat('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
}).format(date);
---

<div class="flex flex-wrap items-center gap-3 text-sm text-gray-4">
  <time datetime={date.toISOString()}>{formatted}</time>
  {tags && tags.length > 0 && (
    <>
      <span class="text-gray-3">/</span>
      {tags.map((tag) => (
        <span class="text-purple">{tag}</span>
      ))}
    </>
  )}
</div>
